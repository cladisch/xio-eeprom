The xio-eeprom tool allows to access the EEPROM of Texas Instruments XIOxxxx
devices in Linux.

To compile, execute "make xio-eeprom".
There are no configuration or installation scripts.

Run "xio-eeprom --help" to get a list of commands and options.
Not all features of the original OHCIROM tool are supported.

To be able to access the hardware, this tool needs to be run with root
privileges.
